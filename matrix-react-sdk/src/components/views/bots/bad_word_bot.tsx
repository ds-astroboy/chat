import React, { useEffect, useState } from "react";

let Filter = require('bad-words');

export const checkBadWords = (content: string) => {
    // var customFilter = new Filter({ placeHolder: 'x', regex: /\*|\.|$/gi, replaceRegex:  /[A-Za-z0-9가-힣_]/g});
    // content = customFilter.clean(content);
    
}